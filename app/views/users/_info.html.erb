<table class='table'>
  <tr><%= image_tag user.get_profile_image, size:'100x100' %></tr>
  <tr>
	  <th>name</th>
	  <th><%= user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= user.introduction %></th>
  </tr>
  <tr>
    <th>follows</th>
    <th><%= link_to user.followings.count, user_followings_path(user) %></th>
  </tr>
  <tr>
    <th>followers</th>
    <th><%= link_to user.followers.count, user_followers_path(user) %></th>
  </tr>
</table>
　
<div class='width-100'>
  <div class='row'>
  <% if current_user != user %>
    <span><%= render "relationships/btn", user: user %></span>
    <span><%= render "chat", user: @user, isRoom: @isRoom, roomId: @roomId, room: @room, entry: @entry %></span>
  <% end %>
  </div>
    <%= link_to edit_user_path(user), class: "mt-1 btn btn-outline-secondary btn-block edit_user_#{user.id}" do %>
      <i class="fas fa-user-cog"></i>
    <% end %>
</div>